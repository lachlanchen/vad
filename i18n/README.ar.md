[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# اكتشاف نشاط الصوت لجافاسكربت

[![npm vad-web](https://img.shields.io/npm/v/@ricky0123/vad-web?color=0b69d7&label=%40ricky0123%2Fvad-web&style=flat-square)](https://www.npmjs.com/package/@ricky0123/vad-web)
[![npm vad-react](https://img.shields.io/npm/v/@ricky0123/vad-react?color=0b69d7&label=%40ricky0123%2Fvad-react&style=flat-square)](https://www.npmjs.com/package/@ricky0123/vad-react)
[![Docs](https://img.shields.io/badge/docs-vad.ricky0123.com-0a7f5a?style=flat-square)](https://docs.vad.ricky0123.com/)
[![Demo](https://img.shields.io/badge/demo-live-ff8c00?style=flat-square)](https://www.vad.ricky0123.com)
[![Discord](https://img.shields.io/badge/discord-community-5865F2?style=flat-square&logo=discord&logoColor=white)](https://discord.gg/4WPeGEaSpF)
[![License: ISC](https://img.shields.io/badge/license-ISC-2ea44f?style=flat-square)](LICENSE)

> شغّل دوال الاستدعاء على مقاطع الصوت التي تحتوي على كلام المستخدم في بضعة أسطر برمجية.

تهدف هذه الحزمة إلى توفير كاشف نشاط صوت (VAD) دقيق وسهل الاستخدام يعمل داخل المتصفح. باستخدام هذه الحزمة يمكنك طلب صلاحية الميكروفون من المستخدم، وبدء تسجيل الصوت، وإرسال المقاطع التي تحتوي على كلام إلى خادمك للمعالجة، أو إظهار حركة/مؤشر معيّن عندما يتحدث المستخدم. لاحظ أنني قررت [إيقاف دعم node](#تحديث-مهم-حول-دعم-node---أكتوبر-2024-) للتركيز على حالة استخدام المتصفح.

## جدول المحتويات

- [روابط سريعة 🔗](#روابط-سريعة-)
- [نظرة عامة 🧭](#نظرة-عامة-)
- [الميزات ✨](#الميزات-)
- [هيكل المشروع 🗂️](#هيكل-المشروع-️)
- [مصفوفة التوافق 🧩](#مصفوفة-التوافق-)
- [المتطلبات المسبقة ✅](#المتطلبات-المسبقة-)
- [التثبيت 📦](#التثبيت-)
- [الاستخدام 🚀](#الاستخدام-)
- [الإعداد ⚙️](#الإعداد-️)
- [أمثلة 🧪](#أمثلة-)
- [ملاحظات التطوير 🛠️](#ملاحظات-التطوير-️)
- [CI وبوابات الجودة 🧱](#ci-وبوابات-الجودة-)
- [استكشاف الأخطاء وإصلاحها 🩺](#استكشاف-الأخطاء-وإصلاحها-)
- [الرعاية ❤️](#الرعاية-️)
- [تحديث مهم حول دعم node - أكتوبر 2024 📢](#تحديث-مهم-حول-دعم-node---أكتوبر-2024-)
- [خارطة الطريق 🛣️](#خارطة-الطريق-️)
- [المساهمة 🤝](#المساهمة-)
- [المراجع 📚](#المراجع-)
- [الترخيص 📄](#الترخيص-)

## روابط سريعة 🔗

| المورد | الرابط |
| --- | --- |
| العرض الحي | [vad.ricky0123.com](https://www.vad.ricky0123.com) |
| التوثيق | [docs.vad.ricky0123.com](https://docs.vad.ricky0123.com/) |
| Discord | [انضم إلى المجتمع](https://discord.gg/4WPeGEaSpF) |
| الاستبيان | [شارك حالة الاستخدام الخاصة بك](https://uaux2a2ppfv.typeform.com/to/iJG2gCQv) |
| دليل المساهمة | [دليل التطوير للمساهمين](https://docs.vad.ricky0123.com/developer-guide/hacking/) |

- تصفّح التوثيق، ومصدره موجود في المجلد `./docs`.
- إذا كنت ترغب في المساهمة، فقد بدأت بكتابة توثيق يشرح كيفية البدء بالعمل على هذه الحزم [هنا](https://docs.vad.ricky0123.com/developer-guide/hacking/). إذا كان لديك أي سؤال، يمكنك فتح issue هنا أو ترك رسالة على Discord.

في الخلفية، تشغّل هذه الحزم [Silero VAD](https://github.com/snakers4/silero-vad) [[1]](#المراجع) باستخدام [ONNX Runtime Web](https://github.com/microsoft/onnxruntime/tree/main/js/web) / [ONNX Runtime Node.js](https://github.com/microsoft/onnxruntime/tree/main/js/node). شكرًا جزيلاً لهؤلاء على إتاحة ذلك.

ملاحظة حول حالة i18n: المجلد `i18n/` موجود ويتضمن عدة ملفات README مترجمة. يتضمن محدد اللغة أعلاه أيضًا روابط لترجمات مخطط لها/بديلة (`README.de.md`, `README.ru.md`) قد لا تكون موجودة في هذه اللقطة من المستودع.

## نظرة عامة 🧭

هذا المستودع هو monorepo ويضم حزمتين منشورتين أساسيتين:

| الحزمة | الغرض |
| --- | --- |
| `@ricky0123/vad-web` | واجهات المتصفح بما في ذلك `MicVAD` و`AudioNodeVAD` و`NonRealTimeVAD` |
| `@ricky0123/vad-react` | غلاف React hook (`useMicVAD`) لحزمة `vad-web` |

المشروع موجّه للمتصفح أولًا ويتضمن:

- دوال استدعاء لتقسيم الصوت من الميكروفون في الزمن الحقيقي (`onSpeechStart`, `onSpeechEnd`, `onVADMisfire`، إلخ)
- عتبات خوارزمية قابلة للتهيئة وعناصر تحكم زمنية
- دعم نموذج Silero القديم وإصدار v5
- تطبيقات تجريب/اختبار ومصادر موقع التوثيق ضمن هذا المستودع

## الميزات ✨

- مسار VAD موجّه للمتصفح ومدعوم بنماذج Silero ONNX
- يعمل مع script tags وأدوات bundlers وReact
- قيود افتراضية مناسبة لتدفق الميكروفون
- إمكانية تجاوز دورة حياة التدفق (`getStream`, `pauseStream`, `resumeStream`)
- تقسيم الكلام خارج الزمن الحقيقي للصوت المسجّل مسبقًا عبر `NonRealTimeVAD`
- إعداد تحميل النموذج/الأصول عبر `baseAssetPath` و`onnxWASMBasePath`
- يدعم التعامل مع حالة النموذج القديم وv5 عبر wrappers مدمجة
- يتضمن أمثلة لـ script tags، وbundlers المعتمدة على webpack، وReact bundlers، وNext.js

## هيكل المشروع 🗂️

```text
.
├── README.md
├── docs/                     # MkDocs source for docs.vad.ricky0123.com
├── examples/                 # script-tag, bundler, react-bundler, nextjs examples
├── packages/
│   ├── web/                  # @ricky0123/vad-web
│   └── react/                # @ricky0123/vad-react
├── scripts/                  # dev helpers
├── test-site/                # local interactive playground
├── i18n/                     # translated README files
├── silero_vad_legacy.onnx
└── silero_vad_v5.onnx
```

مسارات أكثر تفصيلًا:

- `packages/web/src/real-time-vad.ts`: تنسيق VAD للميكروفون/عقدة الصوت في الزمن الحقيقي
- `packages/web/src/non-real-time-vad.ts`: تقسيم غير متزامن للصوت المسجّل مسبقًا
- `packages/web/src/frame-processor.ts`: منطق العتبات وحدود مقاطع الكلام
- `packages/react/src/index.ts`: دورة الحياة وغلاف الحالة لـ `useMicVAD` في React

## مصفوفة التوافق 🧩

| المكوّن | البيئة |
| --- | --- |
| `@ricky0123/vad-web` | متصفحات حديثة تدعم WebAudio و`MediaDevices.getUserMedia` |
| `@ricky0123/vad-react` | تطبيقات React (`react` / `react-dom` >= 16.8.0) |
| سلسلة أدوات التوثيق | Python 3.10 + Poetry (حسب سير عمل CI) |
| بيئة Node في CI | Node 18 (حسب سير عمل المستودع) |

ملاحظة افتراضية: الأمثلة والتوثيق متسقان مع إصدارات الحزم الحالية في هذه اللقطة من المستودع (`@ricky0123/vad-web@0.0.27`, `@ricky0123/vad-react@0.0.33`).

## المتطلبات المسبقة ✅

- استخدام المتصفح: متصفح حديث يدعم `MediaDevices.getUserMedia`
- التطوير المحلي: Node.js + npm workspaces
- تطوير التوثيق: Python + Poetry (لبناء MkDocs)

الحد الأدنى المحلي الموصى به بناءً على إعداد CI:

- Node.js 18.x
- Python 3.10.x

## التثبيت 📦

ثبّت حزمة المتصفح:

```bash
npm i @ricky0123/vad-web
```

ثبّت غلاف React:

```bash
npm i @ricky0123/vad-react
```

ثبّت اعتماديات الـ monorepo (للمساهمين):

```bash
npm install
```

## الاستخدام 🚀

### بداية سريعة (script tags)

لاستخدام VAD عبر script tag في المتصفح، أضف وسوم السكربت التالية:

```html
<script src="https://cdn.jsdelivr.net/npm/onnxruntime-web@1.22.0/dist/ort.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@ricky0123/vad-web@0.0.27/dist/bundle.min.js"></script>
<script>
  async function main() {
    const myvad = await vad.MicVAD.new({
      onSpeechStart: () => {
        console.log("Speech start detected")
      },
      onSpeechEnd: (audio) => {
        // do something with `audio` (Float32Array of audio samples at sample rate 16000)...
      },
      onnxWASMBasePath: "https://cdn.jsdelivr.net/npm/onnxruntime-web@1.22.0/dist/",
      baseAssetPath: "https://cdn.jsdelivr.net/npm/@ricky0123/vad-web@0.0.27/dist/",
    })
    myvad.start()
  }
  main()
</script>
```

### استخدام حزمة المتصفح (module import)

```ts
import { MicVAD } from "@ricky0123/vad-web"

const myvad = await MicVAD.new({
  onSpeechEnd: (audio) => {
    console.log("Speech segment length:", audio.length)
  },
})

myvad.start()
```

### استخدام React

```tsx
import { useMicVAD } from "@ricky0123/vad-react"

export function MyComponent() {
  const vad = useMicVAD({
    onSpeechEnd: (audio) => {
      console.log("User stopped talking", audio.length)
    },
  })

  return <div>{vad.userSpeaking ? "User is speaking" : "Idle"}</div>
}
```

### استخدام خارج الزمن الحقيقي (batch audio)

```ts
import { NonRealTimeVAD } from "@ricky0123/vad-web"

const myvad = await NonRealTimeVAD.new()
for await (const { audio, start, end } of myvad.run(audioData, sampleRate)) {
  console.log({ start, end, samples: audio.length })
}
```

## الإعداد ⚙️

تشمل الخيارات الشائعة عبر الواجهات البرمجية:

- `positiveSpeechThreshold` (القيمة الافتراضية قرابة `0.3` في واجهات الزمن الحقيقي)
- `negativeSpeechThreshold` (القيمة الافتراضية قرابة `0.25` في واجهات الزمن الحقيقي)
- `redemptionMs` (القيمة الافتراضية قرابة `1400` في واجهات الزمن الحقيقي)
- `preSpeechPadMs` (القيمة الافتراضية قرابة `800` في واجهات الزمن الحقيقي)
- `minSpeechMs` (القيمة الافتراضية قرابة `400` في واجهات الزمن الحقيقي)

واجهات الزمن الحقيقي (`MicVAD`, `useMicVAD`) تدعم أيضًا:

- `getStream`, `pauseStream`, `resumeStream`
- `onFrameProcessed`, `onSpeechStart`, `onSpeechRealStart`, `onSpeechEnd`, `onVADMisfire`
- `submitUserSpeechOnPause`
- `model` (`"legacy"` or `"v5"`)
- `baseAssetPath` and `onnxWASMBasePath`
- `workletOptions`

اطّلع على جداول API الكاملة في التوثيق: [مرجع API](https://docs.vad.ricky0123.com/user-guide/api/) و[دليل الخوارزمية](https://docs.vad.ricky0123.com/user-guide/algorithm/).

### وصفة إعداد: استضافة النموذج وأصول runtime ذاتيًا

عند عدم استخدام إعدادات CDN الافتراضية، تأكد أن تطبيقك يقدّم:

- `silero_vad_legacy.onnx` و/أو `silero_vad_v5.onnx`
- `vad.worklet.bundle.min.js`
- ملفات runtime الخاصة بـ `onnxruntime-web` (`.wasm`، و`.mjs` للإصدارات الأحدث من runtime)

ثم اضبط الإعدادات:

```ts
const vad = await MicVAD.new({
  baseAssetPath: "/assets/vad/",
  onnxWASMBasePath: "/assets/onnxruntime/",
  onSpeechEnd: (audio) => {
    // handle audio segment
  },
})
```

## أمثلة 🧪

أمثلة المستودع:

- `examples/script-tags`: إعداد أساسي باستخدام script-tag
- `examples/bundler`: webpack + `@ricky0123/vad-web`
- `examples/react-bundler`: webpack + `@ricky0123/vad-react`
- `examples/nextjs`: مثال تكامل Next.js

أمر مثال من `examples/bundler`:

```bash
npm run build && npm run start
```

يمكن العثور على توثيق تجميع (bundling) كاشف نشاط الصوت للمتصفح أو استخدامه في مشاريع node أو React على [vad.ricky0123.com](https://www.vad.ricky0123.com).

## ملاحظات التطوير 🛠️

سكربتات مساحة العمل الجذرية:

```bash
npm run build
npm run test
npm run test:coverage
npm run typecheck
npm run format-check
npm run dev
```

وظيفة كل منها:

- `npm run build`: يبني جميع مساحات العمل
- `npm run test`: يشغّل اختبارات مساحات العمل
- `npm run test:coverage`: تغطية الاختبارات لـ `packages/web`
- `npm run typecheck`: يفحص TypeScript في الحزم و`test-site` والاختبارات
- `npm run format-check`: يفحص التنسيق لملفات TS/TSX ضمن `packages` و`examples` و`test-site`
- `npm run dev`: يراقب مصادر الحزم و`test-site`، ويعيد البناء، ويخدم `test-site/dist`

بناء التوثيق (MkDocs + Poetry):

```bash
poetry install
poetry run mkdocs serve
```

ملاحظات إضافية:

- `./test-site/build.sh` ينسخ أصول VAD/ONNX Runtime المطلوبة إلى `test-site/dist` و`test-site/dist/subpath`
- `./scripts/dev.sh` يستخدم `nodemon` + `live-server` لدورات إعادة البناء والخدمة محليًا على المنفذ `8080`
- `./check_vad_up_to_date.sh` ملف تاريخي ويشير إلى `silero_vad.onnx` (بينما هذا المستودع يوفّر `silero_vad_legacy.onnx` و`silero_vad_v5.onnx`)

## CI وبوابات الجودة 🧱

تغطي سير عمل GitHub في `.github/workflows/` ما يلي:

- الاختبارات (`test.yml`)
- فحص الأنواع (`typecheck.yml`)
- فحص التنسيق (`format-check.yml`)
- بناء/نشر التوثيق (`docs.yml`)
- مسار النشر (`publish.yml`)

تُعد هذه السير مصدرًا عمليًا موثوقًا لإصدارات بيئات التشغيل/الأدوات المتوقعة وفحوصات الإصدار.

## استكشاف الأخطاء وإصلاحها 🩺

| العرض | الفحص / الحل |
| --- | --- |
| تم رفض صلاحية الميكروفون | تأكد أن المتصفح يملك إذن استخدام الميكروفون لمصدر موقعك. |
| فشل تحميل الأصول (`.onnx`, `.wasm`, `.mjs`, worklet) | اضبط `baseAssetPath` / `onnxWASMBasePath` بشكل صحيح وتحقق أن الملفات تُخدَّم فعليًا. |
| مشكلات مع إصدارات أحدث من `onnxruntime-web` | قدّم أيضًا ملفات `.mjs` وليس `.wasm` فقط. |
| تطوير محلي على مصدر غير آمن | عادةً تتطلب واجهات ميكروفون المتصفح سياقات آمنة (`https` أو `localhost`). |
| مشكلات bundler وقت البناء | استخدم إرشادات التجميع في [توثيق المتصفح](https://docs.vad.ricky0123.com/user-guide/browser/). |
| مشكلات تكامل Next.js | استخدم أنماط الإعداد الموضحة في [`examples/nextjs/next.config.js`](examples/nextjs/next.config.js) وتحقق من مسارات استضافة الأصول الثابتة. |

## الرعاية ❤️

يرجى دعم المشروع ماليًا، خاصةً إذا كان منتجك التجاري يعتمد على هذه الحزمة. [![Become a Sponsor](https://img.shields.io/static/v1?label=Become%20a%20Sponsor&message=%E2%9D%A4&logo=GitHub&style=flat&color=d42f2d)](https://github.com/sponsors/ricky0123)

## تحديث مهم حول دعم node - أكتوبر 2024 📢

سأبدأ بتقليل دعم `ricky0123/vad-node`، وهي حزمة اكتشاف نشاط الصوت لبيئات node على الخادم. لا أخطط لنشر أي تحديثات على حزمة node من الآن فصاعدًا. اتخذت هذا القرار للأسباب التالية:

- كانت حالة الاستخدام الأصلية لهذا المشروع هي اكتشاف نشاط الصوت على جهة العميل. أضفت دعم node لأن أحدهم طلب ذلك وأردت المساعدة. لكن ليس لدي وقت كبير للعمل على هذا المشروع، وإيقاف `ricky0123/vad-node` يمنحني وقتًا أكبر للتركيز على `ricky0123/vad-web`.
- من الأسهل بكثير على المطورين الأفراد إنشاء حلول مخصصة لاكتشاف نشاط الصوت على الخادم مقارنةً بتعلّم كيفية العمل مع onnxruntime-web وaudio worklets وتقنيات أخرى لإنتاج حل على جهة العميل. لذلك أرى أن `ricky0123/vad-web` يقدم قيمة أكبر للمجتمع.
- مشاركة الشيفرة بين حزمتَي المتصفح وnode غير مريحة نسبيًا لأن البيئتين مختلفتان بطرق تؤثر على تشغيل نموذج اكتشاف نشاط الصوت واستخدامه.
- معظم المستخدمين، وفقًا لـ [الاستبيان](https://uaux2a2ppfv.typeform.com/to/iJG2gCQv)، يستخدمون `ricky0123/vad-web` (وربما مع `ricky0123/vad-react`).

## خارطة الطريق 🛣️

الاتجاه الحالي (استنادًا إلى حالة المستودع وملاحظة المشرف أعلاه):

- الاستمرار في التركيز على واجهات API الموجّهة للمتصفح (`@ricky0123/vad-web`, `@ricky0123/vad-react`)
- صيانة وتحسين التوثيق/الأمثلة الخاصة بـ bundlers والأطر
- تحسين توثيق المطورين/المساهمين ومسارات عمل test-site
- إضافة وصيانة ملفات README المترجمة ضمن `i18n/`

## المساهمة 🤝

- اقرأ دليل التطوير: [docs.vad.ricky0123.com/developer-guide/hacking](https://docs.vad.ricky0123.com/developer-guide/hacking/)
- افتح issues أو PRs في هذا المستودع: [github.com/ricky0123/vad/issues](https://github.com/ricky0123/vad/issues)
- لالتقاط سياق المشروع بسرعة، راجع [`HACKING.md`](HACKING.md)

## المراجع 📚

1. مستودع Silero VAD: [github.com/snakers4/silero-vad](https://github.com/snakers4/silero-vad)

## الترخيص 📄

- ترخيص المشروع: ISC (راجع [LICENSE](LICENSE))
