[English](../README.md) · [العربية](README.ar.md) · [Español](README.es.md) · [Français](README.fr.md) · [日本語](README.ja.md) · [한국어](README.ko.md) · [Tiếng Việt](README.vi.md) · [中文 (简体)](README.zh-Hans.md) · [中文（繁體）](README.zh-Hant.md) · [Deutsch](README.de.md) · [Русский](README.ru.md)


# Детектор голосовой активности для JavaScript

[![npm vad-web](https://img.shields.io/npm/v/@ricky0123/vad-web?color=0b69d7&label=%40ricky0123%2Fvad-web&style=flat-square)](https://www.npmjs.com/package/@ricky0123/vad-web)
[![npm vad-react](https://img.shields.io/npm/v/@ricky0123/vad-react?color=0b69d7&label=%40ricky0123%2Fvad-react&style=flat-square)](https://www.npmjs.com/package/@ricky0123/vad-react)
[![Docs](https://img.shields.io/badge/docs-vad.ricky0123.com-0a7f5a?style=flat-square)](https://docs.vad.ricky0123.com/)
[![Demo](https://img.shields.io/badge/demo-live-ff8c00?style=flat-square)](https://www.vad.ricky0123.com)
[![Discord](https://img.shields.io/badge/discord-community-5865F2?style=flat-square&logo=discord&logoColor=white)](https://discord.gg/4WPeGEaSpF)
[![License: ISC](https://img.shields.io/badge/license-ISC-2ea44f?style=flat-square)](LICENSE)

> Запускайте колбэки на фрагментах аудио с речью пользователя всего в нескольких строках кода.

Этот пакет предназначен для точного и удобного детектора голосовой активности (VAD), работающего в браузере. С его помощью можно запросить у пользователя доступ к микрофону, начать запись аудио, отправлять на сервер фрагменты с речью для обработки или показывать анимацию/индикатор, когда пользователь говорит. Обратите внимание: я решил [прекратить поддержку node](#важное-обновление-о-поддержке-node---октябрь-2024-) и сосредоточиться на браузерном сценарии.

## Содержание

- [Быстрые ссылки 🔗](#быстрые-ссылки-)
- [Обзор 🧭](#обзор-)
- [Возможности ✨](#возможности-)
- [Структура проекта 🗂️](#структура-проекта-)
- [Матрица совместимости 🧩](#матрица-совместимости-)
- [Предварительные требования ✅](#предварительные-требования-)
- [Установка 📦](#установка-)
- [Использование 🚀](#использование-)
- [Конфигурация ⚙️](#конфигурация-)
- [Примеры 🧪](#примеры-)
- [Заметки по разработке 🛠️](#заметки-по-разработке-)
- [CI и контроль качества 🧱](#ci-и-контроль-качества-)
- [Устранение неполадок 🩺](#устранение-неполадок-)
- [Спонсорство ❤️](#спонсорство-)
- [Важное обновление о поддержке node — октябрь 2024 📢](#важное-обновление-о-поддержке-node---октябрь-2024-)
- [Дорожная карта 🛣️](#дорожная-карта-)
- [Участие в разработке 🤝](#участие-в-разработке-)
- [Ссылки 📚](#ссылки-)
- [Лицензия 📄](#лицензия-)

## Быстрые ссылки 🔗

| Ресурс | Ссылка |
| --- | --- |
| Live demo | [vad.ricky0123.com](https://www.vad.ricky0123.com) |
| Документация | [docs.vad.ricky0123.com](https://docs.vad.ricky0123.com/) |
| Discord | [Присоединиться к сообществу](https://discord.gg/4WPeGEaSpF) |
| Опрос | [Поделитесь вашим кейсом](https://uaux2a2ppfv.typeform.com/to/iJG2gCQv) |
| Руководство по вкладу | [Руководство для разработчиков](https://docs.vad.ricky0123.com/developer-guide/hacking/) |

- Изучайте документацию, исходный код которой находится в каталоге `./docs`.
- Если хотите внести вклад, я начал писать документацию о том, как начать работу с этими пакетами, [здесь](https://docs.vad.ricky0123.com/developer-guide/hacking/). Если есть вопросы, можно открыть issue в этом репозитории или написать в Discord.

Под капотом эти пакеты используют [Silero VAD](https://github.com/snakers4/silero-vad) [[1]](#ссылки) через [ONNX Runtime Web](https://github.com/microsoft/onnxruntime/tree/main/js/web) / [ONNX Runtime Node.js](https://github.com/microsoft/onnxruntime/tree/main/js/node). Большое спасибо авторам этих проектов.

Примечание о состоянии i18n: каталог `i18n/` существует и содержит несколько переведенных файлов README. Селектор языков выше также включает ссылки на планируемые/временные переводы (`README.de.md`, `README.ru.md`), которых может не быть в текущем снимке репозитория.

## Обзор 🧭

Этот репозиторий представляет собой монорепозиторий с двумя основными опубликованными пакетами:

| Пакет | Назначение |
| --- | --- |
| `@ricky0123/vad-web` | Browser API, включая `MicVAD`, `AudioNodeVAD` и `NonRealTimeVAD` |
| `@ricky0123/vad-react` | React-обертка-хук (`useMicVAD`) для `vad-web` |

Проект ориентирован в первую очередь на браузер и включает:

- Колбэки сегментации микрофона в реальном времени (`onSpeechStart`, `onSpeechEnd`, `onVADMisfire` и т. д.)
- Настраиваемые пороги алгоритма и параметры времени
- Поддержку устаревшей и v5 моделей Silero
- Исходники demo/test-приложений и сайта документации в этом репозитории

## Возможности ✨

- Browser-first VAD-конвейер на базе моделей Silero ONNX
- Работает через script tags, с bundler-ами и в React
- Разумные значения по умолчанию для ограничений потока микрофона
- Переопределяемый жизненный цикл потока (`getStream`, `pauseStream`, `resumeStream`)
- Нереалтайм-сегментация речи для заранее записанного аудио через `NonRealTimeVAD`
- Настраиваемая загрузка модели/ассетов через `baseAssetPath` и `onnxWASMBasePath`
- Поддержка как legacy, так и v5 обработки состояния модели через встроенные обертки
- Примеры для script tags, bundler-ов на webpack, React bundler-ов и Next.js

## Структура проекта 🗂️

```text
.
├── README.md
├── docs/                     # MkDocs source for docs.vad.ricky0123.com
├── examples/                 # script-tag, bundler, react-bundler, nextjs examples
├── packages/
│   ├── web/                  # @ricky0123/vad-web
│   └── react/                # @ricky0123/vad-react
├── scripts/                  # dev helpers
├── test-site/                # local interactive playground
├── i18n/                     # translated README files
├── silero_vad_legacy.onnx
└── silero_vad_v5.onnx
```

Более детально по путям:

- `packages/web/src/real-time-vad.ts`: оркестрация VAD для микрофона/аудио-ноды в реальном времени
- `packages/web/src/non-real-time-vad.ts`: асинхронная сегментация заранее записанного аудио
- `packages/web/src/frame-processor.ts`: логика порогов и границ речевых сегментов
- `packages/react/src/index.ts`: жизненный цикл React-хука `useMicVAD` и обертка состояния

## Матрица совместимости 🧩

| Компонент | Окружение |
| --- | --- |
| `@ricky0123/vad-web` | Современные браузеры с WebAudio + `MediaDevices.getUserMedia` |
| `@ricky0123/vad-react` | React-приложения (`react` / `react-dom` >= 16.8.0) |
| Toolchain документации | Python 3.10 + Poetry (согласно CI workflow) |
| Node runtime в CI | Node 18 (согласно workflow репозитория) |

Примечание по допущениям: примеры и документация соответствуют текущим версиям пакетов в этом снимке репозитория (`@ricky0123/vad-web@0.0.27`, `@ricky0123/vad-react@0.0.33`).

## Предварительные требования ✅

- Использование в браузере: современный браузер с `MediaDevices.getUserMedia`
- Локальная разработка: Node.js + npm workspaces
- Разработка документации: Python + Poetry (для сборки MkDocs)

Рекомендуемая локальная база, исходя из CI-конфигурации:

- Node.js 18.x
- Python 3.10.x

## Установка 📦

Установка браузерного пакета:

```bash
npm i @ricky0123/vad-web
```

Установка React-обертки:

```bash
npm i @ricky0123/vad-react
```

Установка зависимостей монорепозитория (для контрибьюторов):

```bash
npm install
```

## Использование 🚀

### Быстрый старт (script tags)

Чтобы использовать VAD в браузере через script tag, подключите следующие теги:

```html
<script src="https://cdn.jsdelivr.net/npm/onnxruntime-web@1.22.0/dist/ort.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@ricky0123/vad-web@0.0.27/dist/bundle.min.js"></script>
<script>
  async function main() {
    const myvad = await vad.MicVAD.new({
      onSpeechStart: () => {
        console.log("Speech start detected")
      },
      onSpeechEnd: (audio) => {
        // do something with `audio` (Float32Array of audio samples at sample rate 16000)...
      },
      onnxWASMBasePath: "https://cdn.jsdelivr.net/npm/onnxruntime-web@1.22.0/dist/",
      baseAssetPath: "https://cdn.jsdelivr.net/npm/@ricky0123/vad-web@0.0.27/dist/",
    })
    myvad.start()
  }
  main()
</script>
```

### Использование браузерного пакета (импорт модуля)

```ts
import { MicVAD } from "@ricky0123/vad-web"

const myvad = await MicVAD.new({
  onSpeechEnd: (audio) => {
    console.log("Speech segment length:", audio.length)
  },
})

myvad.start()
```

### Использование в React

```tsx
import { useMicVAD } from "@ricky0123/vad-react"

export function MyComponent() {
  const vad = useMicVAD({
    onSpeechEnd: (audio) => {
      console.log("User stopped talking", audio.length)
    },
  })

  return <div>{vad.userSpeaking ? "User is speaking" : "Idle"}</div>
}
```

### Использование не в реальном времени (batch audio)

```ts
import { NonRealTimeVAD } from "@ricky0123/vad-web"

const myvad = await NonRealTimeVAD.new()
for await (const { audio, start, end } of myvad.run(audioData, sampleRate)) {
  console.log({ start, end, samples: audio.length })
}
```

## Конфигурация ⚙️

Общие параметры API включают:

- `positiveSpeechThreshold` (по умолчанию около `0.3` в API реального времени)
- `negativeSpeechThreshold` (по умолчанию около `0.25` в API реального времени)
- `redemptionMs` (по умолчанию около `1400` в API реального времени)
- `preSpeechPadMs` (по умолчанию около `800` в API реального времени)
- `minSpeechMs` (по умолчанию около `400` в API реального времени)

API реального времени (`MicVAD`, `useMicVAD`) также поддерживают:

- `getStream`, `pauseStream`, `resumeStream`
- `onFrameProcessed`, `onSpeechStart`, `onSpeechRealStart`, `onSpeechEnd`, `onVADMisfire`
- `submitUserSpeechOnPause`
- `model` (`"legacy"` или `"v5"`)
- `baseAssetPath` и `onnxWASMBasePath`
- `workletOptions`

Полные таблицы API см. в документации: [API reference](https://docs.vad.ricky0123.com/user-guide/api/) и [algorithm guide](https://docs.vad.ricky0123.com/user-guide/algorithm/).

### Рецепт конфигурации: self-hosting модели и runtime-ассетов

Если вы не используете CDN-настройки по умолчанию, убедитесь, что приложение раздает:

- `silero_vad_legacy.onnx` и/или `silero_vad_v5.onnx`
- `vad.worklet.bundle.min.js`
- runtime-файлы `onnxruntime-web` (`.wasm`; и `.mjs` для новых сборок runtime)

Затем настройте:

```ts
const vad = await MicVAD.new({
  baseAssetPath: "/assets/vad/",
  onnxWASMBasePath: "/assets/onnxruntime/",
  onSpeechEnd: (audio) => {
    // handle audio segment
  },
})
```

## Примеры 🧪

Примеры в репозитории:

- `examples/script-tags`: базовая настройка с script tags
- `examples/bundler`: webpack + `@ricky0123/vad-web`
- `examples/react-bundler`: webpack + `@ricky0123/vad-react`
- `examples/nextjs`: пример интеграции с Next.js

Пример команды из `examples/bundler`:

```bash
npm run build && npm run start
```

Документацию о бандлинге детектора голосовой активности для браузера или использовании в node/React-проектах можно найти на [vad.ricky0123.com](https://www.vad.ricky0123.com).

## Заметки по разработке 🛠️

Скрипты корневого workspace:

```bash
npm run build
npm run test
npm run test:coverage
npm run typecheck
npm run format-check
npm run dev
```

Что они делают:

- `npm run build`: собирает все workspace-ы
- `npm run test`: запускает тесты workspace-ов
- `npm run test:coverage`: покрытие для `packages/web`
- `npm run typecheck`: проверяет TypeScript в пакетах, test-site и тестах
- `npm run format-check`: проверяет форматирование TS/TSX в `packages`, `examples`, `test-site`
- `npm run dev`: следит за исходниками пакетов и test-site, пересобирает и запускает `test-site/dist`

Сборка документации (MkDocs + Poetry):

```bash
poetry install
poetry run mkdocs serve
```

Дополнительные заметки:

- `./test-site/build.sh` копирует необходимые ассеты VAD/ONNX Runtime в `test-site/dist` и `test-site/dist/subpath`
- `./scripts/dev.sh` использует `nodemon` + `live-server` для локального цикла пересборки и запуска на порту `8080`
- `./check_vad_up_to_date.sh` исторический и ссылается на `silero_vad.onnx` (в этом репозитории используются `silero_vad_legacy.onnx` и `silero_vad_v5.onnx`)

## CI и контроль качества 🧱

GitHub workflow в `.github/workflows/` покрывают:

- Test (`test.yml`)
- Typecheck (`typecheck.yml`)
- Formatting (`format-check.yml`)
- Сборку/деплой документации (`docs.yml`)
- Процесс публикации (`publish.yml`)

Эти workflow являются практическим источником истины для ожидаемых версий runtime/tools и проверок перед релизом.

## Устранение неполадок 🩺

| Симптом | Проверка / решение |
| --- | --- |
| Доступ к микрофону запрещен | Убедитесь, что у браузера есть разрешение на микрофон для вашего origin. |
| Ассеты не загружаются (`.onnx`, `.wasm`, `.mjs`, worklet) | Правильно задайте `baseAssetPath` / `onnxWASMBasePath` и проверьте, что файлы действительно раздаются. |
| Проблемы с новым runtime `onnxruntime-web` | Раздавайте также `.mjs` файлы, а не только `.wasm`. |
| Локальная разработка по небезопасному origin | API микрофона в браузере обычно требуют защищенный контекст (`https` или `localhost`). |
| Проблемы bundler-а на этапе сборки | Используйте рекомендации по бандлингу в [документации для браузера](https://docs.vad.ricky0123.com/user-guide/browser/). |
| Проблемы интеграции с Next.js | Используйте конфигурационные шаблоны из [`examples/nextjs/next.config.js`](examples/nextjs/next.config.js) и проверьте пути хостинга статических ассетов. |

## Спонсорство ❤️

Пожалуйста, поддержите проект финансово, особенно если ваш коммерческий продукт зависит от этого пакета. [![Become a Sponsor](https://img.shields.io/static/v1?label=Become%20a%20Sponsor&message=%E2%9D%A4&logo=GitHub&style=flat&color=d42f2d)](https://github.com/sponsors/ricky0123)

## Важное обновление о поддержке node - октябрь 2024 📢

Я сворачиваю поддержку `ricky0123/vad-node`, пакета детекции голосовой активности для серверных окружений node. Я не планирую публиковать новые обновления node-пакета в дальнейшем. Это решение принято по следующим причинам:

- Изначально мой основной кейс для этого проекта был клиентской детекцией голосовой активности. Поддержку node я добавил по просьбе, чтобы помочь. Однако у меня не так много времени на проект, и отказ от `ricky0123/vad-node` позволит сосредоточиться на `ricky0123/vad-web`.
- Отдельным разработчикам гораздо проще собрать кастомное серверное решение VAD, чем изучать onnxruntime-web, audio worklets и другие технологии для клиентского решения. Поэтому `ricky0123/vad-web`, на мой взгляд, приносит сообществу больше пользы.
- Совместное использование кода между браузерными и node-пакетами довольно неудобно, потому что окружения различаются в аспектах, важных для запуска и использования модели VAD.
- Согласно [опросу](https://uaux2a2ppfv.typeform.com/to/iJG2gCQv), большинство пользователей используют `ricky0123/vad-web` (возможно, вместе с `ricky0123/vad-react`).

## Дорожная карта 🛣️

Текущее направление (по состоянию репозитория и примечанию мейнтейнера выше):

- Продолжать фокус на browser-first API (`@ricky0123/vad-web`, `@ricky0123/vad-react`)
- Поддерживать и улучшать документацию/примеры для bundler-ов и фреймворков
- Улучшать документацию для контрибьюторов/разработчиков и workflow test-site
- Добавлять и поддерживать переведенные README в `i18n/`

## Участие в разработке 🤝

- Прочитайте руководство: [docs.vad.ricky0123.com/developer-guide/hacking](https://docs.vad.ricky0123.com/developer-guide/hacking/)
- Открывайте issue или PR в этом репозитории: [github.com/ricky0123/vad/issues](https://github.com/ricky0123/vad/issues)
- Для быстрого контекста по проекту см. [`HACKING.md`](HACKING.md)

## Ссылки 📚

1. Репозиторий Silero VAD: [github.com/snakers4/silero-vad](https://github.com/snakers4/silero-vad)

## Лицензия 📄

- Лицензия проекта: ISC (см. [LICENSE](LICENSE))
